<div class="page-head"><div><span class="eyebrow dark">cPanel</span><h1><?= e(tr('وضعیت سیستم','System status')) ?></h1><p><?= e(tr('بررسی پیش‌نیازها و تنظیم اجرای دوره‌ای بدون SSH','Check requirements and schedule maintenance without SSH')) ?></p></div><form method="post" action="<?= e(url('maintenance-run')) ?>" data-confirm="<?= e(tr('همگام‌سازی، پایش ترافیک و بررسی انقضا اکنون اجرا شود؟','Run synchronization, traffic polling and expiry checks now?')) ?>"><?= csrf_field() ?><button class="button primary">↻ <?= e(tr('اجرای نگهداری اکنون','Run maintenance now')) ?></button></form></div>
<section class="diagnostic-grid"><?php foreach($diagnostics as $item):?><article class="diagnostic-card"><span class="<?= $item['ok']?'ok':'bad' ?>"><?= $item['ok']?'✓':'×' ?></span><div><small><?= e($item['label']) ?></small><strong><?= e($item['value']) ?></strong></div></article><?php endforeach;?></section>
<section class="panel form-panel"><div class="panel-head"><div><h2><?= e(tr('فایل PHP برای Cron','PHP script for Cron')) ?></h2><p><?= e(tr('برای هاست‌هایی که فقط فایل PHP قبول می‌کنند، این مسیر را انتخاب کنید.','Use this path when the host only accepts PHP scripts.')) ?></p></div></div><div class="cron-box"><code dir="ltr" id="cron-path"><?= e($cronPath) ?></code><button type="button" class="button ghost" onclick="navigator.clipboard.writeText(document.getElementById('cron-path').textContent)"><?= e(tr('کپی مسیر','Copy path')) ?></button></div><div class="instruction"><h3><?= e(tr('تنظیم در cPanel','Configure in cPanel')) ?></h3><ol><li><?= e(tr('بخش Cron Jobs را باز کنید.','Open the Cron Jobs section.')) ?></li><li><?= e(tr('زمان‌بندی هر یک دقیقه را انتخاب کنید.','Choose an every-minute schedule.')) ?> <code dir="ltr">* * * * *</code></li><li><?= e(tr('در قسمت PHP Script همین فایل cron.php یا مسیر بالا را انتخاب کنید.','Choose cron.php or paste the path above in the PHP Script field.')) ?></li></ol><p><?= e(tr('در اجرای مستقیم PHP نیازی به Token یا wget نیست.','Direct PHP execution does not need a token or wget.')) ?></p></div></section>
<section class="panel form-panel"><div class="panel-head"><div><h2><?= e(tr('آدرس Web Cron جایگزین','Alternative Web Cron URL')) ?></h2><p><?= e(tr('فقط برای سرویس‌های Web Cron؛ این URL دارای توکن محرمانه است.','Only for web-cron services; this URL contains a secret token.')) ?></p></div></div><div class="cron-box"><code dir="ltr" id="cron-url"><?= e($cronUrl) ?></code><button type="button" class="button ghost" onclick="navigator.clipboard.writeText(document.getElementById('cron-url').textContent)"><?= e(tr('کپی','Copy')) ?></button></div></section>
